diff --git a/apps/desktop/src/renderer/src/features/composer/Composer.tsx b/apps/desktop/src/renderer/src/features/composer/Composer.tsx
index 75cd590..8e8db42 100644
--- a/apps/desktop/src/renderer/src/features/composer/Composer.tsx
+++ b/apps/desktop/src/renderer/src/features/composer/Composer.tsx
@@ -245,6 +245,16 @@ export function Composer({
   const hasInlineTokens = contextItems.length > 0 || hasSelectedSkills;
   const hasContent = hasText || hasImages || contextItems.length > 0 || hasSelectedSkills;
   const currentModel = models.find((item) => item.id === model) ?? models[0];
+  const effortThinkingOptions = currentModel ? modelThinkingOptions(currentModel) : [];
+  const effortThinkingSelection = currentModel ? selectedThinkingOption(currentModel) : undefined;
+  const discreteEffortOptions = currentModel?.thinkingBudget ? [] : effortThinkingOptions;
+  const effortMaxed = Boolean(
+    currentModel?.supportsThinking &&
+      discreteEffortOptions.length > 1 &&
+      effortThinkingSelection &&
+      discreteEffortOptions[discreteEffortOptions.length - 1]?.value ===
+        effortThinkingSelection.value,
+  );
   const {
     activeIndex,
     isOpen,
@@ -555,7 +565,7 @@ export function Composer({
       {/* biome-ignore lint/a11y/noStaticElementInteractions: drag-drop is a pointer-only enhancement; keyboard users attach images via paste in the editor. */}
       <div
         className={cn(
-          "relative border border-composer-border bg-surface shadow-composer-edge transition-[border-color] duration-150",
+          "composer-prompt-shell relative border border-composer-border bg-surface shadow-composer-edge transition-[border-color] duration-150",
           COMPOSER_RADIUS_CLASS,
           Boolean(footer) && "z-10",
           // No focus glow: only text focus or drag nudges the border one notch brighter.
@@ -563,6 +573,7 @@ export function Composer({
           dragging && "border-composer-border-strong",
           submitting && "pointer-events-none opacity-60",
         )}
+        {...(effortMaxed ? { "data-effort-max": "" } : {})}
         onDragLeave={() => setDragging(false)}
         onDragOver={handleDragOver}
         onDrop={handleDrop}
@@ -828,6 +839,8 @@ function ModelSelect({
 
   const chipClass =
     "app-no-drag inline-flex h-7 min-w-0 flex-none cursor-pointer touch-manipulation items-center gap-1 rounded-lg px-2 text-[12px] font-medium text-fg-muted outline-none transition-colors select-none hover:bg-hover hover:text-fg data-popup-open:bg-hover data-popup-open:text-fg data-disabled:pointer-events-none data-disabled:opacity-45";
+  // Prompt Bar: both model + effort chips turn spark purple at max effort.
+  const chipMaxClass = "text-[color:var(--color-focus-ring-soft)] hover:text-[color:var(--color-focus-ring-soft)]";
 
   if (!current) {
     return (
@@ -846,7 +859,10 @@ function ModelSelect({
   return (
     <div className="flex min-w-0 items-center gap-0.5">
       <Menu.Root>
-        <Menu.Trigger aria-label="Choose model" className={chipClass}>
+        <Menu.Trigger
+          aria-label="Choose model"
+          className={cn(chipClass, effortMaxed && chipMaxClass)}
+        >
           <ProviderLogo
             framed={false}
             name={current.providerName ?? current.provider}
@@ -903,7 +919,7 @@ function ModelSelect({
       <Menu.Root>
         <Menu.Trigger
           aria-label="Choose effort"
-          className={cn(chipClass, effortMaxed && "text-accent hover:text-accent")}
+          className={cn(chipClass, effortMaxed && chipMaxClass)}
           disabled={!effortAvailable || !onModelConfigChange}
         >
           <IconSparkles className="shrink-0" size={13} stroke={2} />
diff --git a/apps/desktop/src/renderer/src/styles/app.css b/apps/desktop/src/renderer/src/styles/app.css
index db79117..69c6a22 100644
--- a/apps/desktop/src/renderer/src/styles/app.css
+++ b/apps/desktop/src/renderer/src/styles/app.css
@@ -771,21 +771,20 @@ body {
   }
 
   /*
-   * Effort energy — mid-gray ink, no outer bloom. Max-tier keeps a bright
-   * internal sheen + sparkle (not an external glow ring).
+   * Effort energy — Prompt Bar spark language: mid-gray ink by default; at max
+   * tier the fill / chip / sparks switch to focus-ring purple (--pb-spark family).
    */
   .effort-energy {
     --effort-ink: light-dark(#3f3f3f, #c4c4c4);
     --effort-ink-mid: light-dark(#555555, #b0b0b0);
     --effort-knob: light-dark(#f0f0f0, #2a2a2a);
-    /* Sparks stay near-white so they punch on both mid-gray fills. */
-    --effort-spark: #ffffff;
+    --effort-spark: var(--color-focus-ring-soft);
   }
 
   .effort-energy-track {
     isolation: isolate;
-    background: color-mix(in srgb, var(--effort-ink) 16%, transparent);
-    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--effort-ink) 10%, transparent);
+    background: color-mix(in srgb, var(--effort-ink) 8%, transparent);
+    box-shadow: none;
   }
 
   .effort-energy-fill {
@@ -795,13 +794,14 @@ body {
     opacity: var(--effort-opacity);
     transition:
       width 210ms var(--ease-out-quint),
-      opacity 180ms var(--ease-out-quint);
+      opacity 180ms var(--ease-out-quint),
+      background 220ms var(--ease-out-quint);
   }
 
   .effort-energy-stop {
     z-index: 2;
-    background: color-mix(in srgb, var(--effort-ink) 55%, transparent);
-    opacity: 0;
+    background: color-mix(in srgb, var(--effort-ink) 30%, transparent);
+    opacity: 1;
     transition: opacity 120ms var(--ease-out-quint);
   }
 
@@ -809,6 +809,10 @@ body {
     opacity: 1;
   }
 
+  .effort-energy-stop:not([data-unfilled="true"]) {
+    opacity: 0;
+  }
+
   .effort-energy-thumb {
     z-index: 3;
     background: var(--effort-knob);
@@ -835,11 +839,21 @@ body {
     scale: 0.9;
   }
 
+  .effort-energy[data-maximum="true"] {
+    --effort-ink: var(--color-focus-ring-soft);
+    --effort-ink-mid: var(--color-focus-ring);
+    --effort-knob: light-dark(#f7f4ff, #1c1528);
+  }
+
   .effort-energy[data-maximum="true"] .effort-energy-thumb {
     box-shadow: 0 0 0 1.5px var(--effort-ink);
   }
 
   .effort-energy[data-maximum="true"] .effort-energy-track {
+    background: color-mix(in srgb, var(--effort-ink) 14%, transparent);
+  }
+
+  .effort-energy[data-maximum="true"] .effort-energy-fill {
     background: linear-gradient(90deg, var(--effort-ink-mid), var(--effort-ink));
   }
 
@@ -893,6 +907,29 @@ body {
     mask-image: linear-gradient(90deg, transparent, #000 14%, #000 86%, transparent);
     animation: effort-energy-particles 520ms var(--ease-out-quint) infinite;
   }
+
+  /* Prompt Bar max-effort field wash (radial spark from bottom-left). */
+  .composer-prompt-shell {
+    isolation: isolate;
+  }
+  .composer-prompt-shell::before {
+    position: absolute;
+    inset: 0;
+    z-index: 0;
+    border-radius: inherit;
+    background: radial-gradient(
+      140% 120% at 0% 100%,
+      color-mix(in srgb, var(--color-focus-ring-soft) 26%, transparent),
+      transparent 62%
+    );
+    opacity: 0;
+    transition: opacity 500ms ease;
+    content: "";
+    pointer-events: none;
+  }
+  .composer-prompt-shell[data-effort-max]::before {
+    opacity: 1;
+  }
 }
 
 @media (prefers-reduced-motion: reduce) {
